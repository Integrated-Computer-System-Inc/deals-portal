{
  "name": "@my-app/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "db:index": "ts-node scripts/apply-indexes.ts",
    "db:deploy": "ts-node scripts/deploy-database.ts",
    "benchmark": "ts-node scripts/benchmark-queries.ts",
    "check:indexes": "ts-node scripts/check-active-indexes.ts",
    "check:rcsi": "ts-node scripts/check-db-info.ts",
    "test:concurrency": "ts-node scripts/test-rcsi-concurrency.ts",
    "seed": "npx ts-node src/seed.ts",
    "seed:admins": "npx ts-node scripts/seed-admins.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prisma": "^5.12.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
